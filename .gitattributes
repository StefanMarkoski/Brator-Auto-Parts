* text=auto

# The bought theme is stored verbatim. Git must not normalise line endings here:
# rtl.css and jquery.js ship with CRLF, and rewriting them would break the
# byte-for-byte guarantee that the storefront styling is unchanged.
public/assets/**            -text
resources/theme-reference/** -text
